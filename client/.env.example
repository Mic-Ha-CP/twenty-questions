# 客户端 env 范例。**本文件只是文档** —— Vite 读的是 .env.local(已 gitignore)。
#
# 后端地址。跨 repo 的 URL 只能来自 env(CONVENTIONS.md),源码里永不写死。
#
#   本地开发   不设即可,默认 http://localhost:3002
#   bring-up  指向 VPS,用来在切生产前打一局真的(VPS-DEPLOY §1.5)
#   生产       在 Vercel 的 Environment Variables 里设,不在这个文件里
#
# ⚠️ Vite 的 env 是**构建期烤进 bundle 的**(VPS-DEPLOY gotcha 4)——
#    在 Vercel 改了值必须重新 deploy 才生效。
# ⚠️ VITE_* 从来不是 secret:它明文躺在发布出去的 JS 里。别往里放任何密钥。
VITE_SERVER_URL=https://tq.fourvirtues.net
